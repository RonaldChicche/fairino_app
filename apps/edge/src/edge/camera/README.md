# camera

Cliente Open GoPro por USB para la HERO10: aplicar la configuración de captura, iniciar y detener la grabación, confirmar que está grabando, listar y descargar el archivo de la toma.

No le corresponde: decidir cuándo grabar (`session/`), recortar o procesar video (`render/`), ni la alimentación física de la cámara.
