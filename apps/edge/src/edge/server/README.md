# server

Servidor HTTP local del edge, en su propia red wifi (D15): interfaz del operador en HTML plano, QR, página local de respaldo para el invitado (D21) y PWA. Funciona sin internet.

No le corresponde: la lógica de la toma (`session/`), ni la página del invitado en la nube (`apps/web`).
