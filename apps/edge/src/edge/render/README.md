# render

Wrapper de ffmpeg que convierte el crudo de una toma en el único archivo final, aplicando la plantilla del evento (D17, D19, D20).

No le corresponde: definir el formato de la plantilla (`packages/contracts`), descargar el crudo de la cámara (`camera/`) ni subir el resultado (`uploader/`).
