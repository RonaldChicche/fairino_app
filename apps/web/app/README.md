# app

Rutas del monolito Next.js en Vercel (D13), subdominio `glam.datzoncompany.com`: página del invitado, galería del cliente, panel de plantillas y API de sesiones. Sin realtime: polling (D14).

No le corresponde: nada del camino crítico del evento (mover, grabar, renderizar), ni la interfaz del operador, que la sirve `apps/edge`.
