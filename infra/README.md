# infra

Aprovisionamiento y despliegue del edge: el mini PC x86 (D12), o la laptop mientras tanto (D4), su contenedor, su arranque y su punto de acceso wifi propio (D15, E1).

No le corresponde: código de aplicación, ni la configuración de Vercel propia de `apps/web`.
