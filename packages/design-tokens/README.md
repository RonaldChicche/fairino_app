# design-tokens

Colores, tipografía y espaciado compartidos por la interfaz del operador (`apps/edge`) y la web (`apps/web`).

No le corresponde: componentes, estilos propios de una pantalla, ni la marca del cliente de cada evento, que va en la plantilla.
